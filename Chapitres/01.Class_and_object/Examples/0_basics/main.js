import App from "./js/App";

let monApplication = new App();
/**
 * LA STRUCTURE DE VOTRE CODE AVEC VITE
 *
 * package.json : les dépendances de votre projet et les scripts de lancement
 * vite.config.js : la configuration de Vite (optionel)
 * .gitignore : les fichiers à ignorer pour Git
 * index.html : la page HTML principale
 * main.js : le point d'entrée de votre application
 * public/ : le dossier contenant les fichiers statiques
 * node_modules/ : le dossier contenant les dépendances de votre projet
 * js/ : le dossier contenant les fichiers JavaScript
 *
 * optionel:
 * scss/ : le dossier contenant les fichiers SCSS (à importer dans le fichier main.js)
 */

// exemple de functionnalités dans le style de P5.js

/**
function setup() {
  createCanvas(400, 400);
  background(255);
}

function draw() {
  fill(0);
  ellipse(mouseX, mouseY, 50, 50);
}

function mousePressed() {
  console.log("mouse pressed");
}

function keyPressed() {
  console.log("key pressed");
}


 */
